;;; emacs/imp/init.el -*- lexical-binding: t; -*-


;;------------------------------------------------------------------------------
;; Usage
;;------------------------------------------------------------------------------

;;------------------
;; Require
;; ------
;; (imp:require <symbol/keyword0> ...)
;;
;;------------------

;;------------------
;; Provide
;; -------
;; (imp:provide <symbol/keyword0> ...)
;;
;;------------------


;;------------------------------------------------------------------------------
;; Load our files...
;;------------------------------------------------------------------------------

;; Debug First!..
(if (featurep! -debug)
    (load! "+debug")

  ;; Fake debug funcs so no one screams.
  (load! "+no-debug"))

;; Order matters.
(load! "error")
(load! "path")
(load! "tree")
(load! "load")


;;------------------------------------------------------------------------------
;; The End.
;;------------------------------------------------------------------------------
(provide 'imp)
